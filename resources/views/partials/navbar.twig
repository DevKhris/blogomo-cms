<nav class="navbar navbar-expand-lg navbar-dark bg-primary">

	<div class="container-fluid">
		<a class="navbar-brand" href="{{ url_for('home') }}">
			<img class="img-fluid" src="{{ full_url_for('home') }}/images/logo_large.png" width="156"/>

		</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav ml-auto">
				<li class="nav-item{% if is_current_url('home') %} active{% endif %}">
					<a class="nav-link" href="{{ url_for('home') }}">
						Home
						{% if is_current_url('home') %}
							<span class="sr-only"></span>
						{% endif %}
					</a>
				</li>
				<li class="nav-item{% if is_current_url('blog') %} active{% endif %}">
					<a class="nav-link" href="{{ url_for('blog') }}">
						Blog
						{% if is_current_url('blog') %}
							<span class="sr-only"></span>
						{% endif %}
					</a>
				</li>
				<li class="nav-item{% if is_current_url('blog') %} active{% endif %}">
					<a class="nav-link" href="{{ url_for('blog') }}">
						About
						{% if is_current_url('blog') %}
							<span class="sr-only"></span>
						{% endif %}
					</a>
				</li>
				<li class="nav-item{% if is_current_url('blog') %} active{% endif %}">
					<a class="nav-link" href="{{ url_for('blog') }}">
						Contact
						{% if is_current_url('blog') %}
							<span class="sr-only"></span>
						{% endif %}
					</a>
				</li>
				<div class="my-2 my-lg-0">
					{%if uinfo %}
						<li class="nav-item dropdown">
							<a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-flip="true" data-bs-toggle="dropdown" data-bs-reference="parent" aria-haspopup="true" aria-expanded="false">
								{{ uinfo.firstname }}
								<i class="fa fa-user-circle"></i>
							</a>
							<ul class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
								<a class="dropdown-item" href="{{ url_for('overview') }}">Dashboard</a>
								<a class="dropdown-item" href="#">Settings</a>
								<div class="dropdown-divider"></div>
								<a class="dropdown-item my-2 my-sm-0" href="{{ url_for('logout') }}">
									<i class="fa fa-sign-in"></i>

									Logout
								</a>
							</ul>
						</li>
					{% endif %}
				</div>
			</div>
		</div>
	</div>
</nav>
